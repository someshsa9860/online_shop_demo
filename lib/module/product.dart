import 'package:/flutter/material.dart';
import 'package:flutter/material.dart';

class _productnit {
  final String image, title, discription;
  final int price, size, id;
  final Color color;

  _productnit(
      {this.image,
      this.title,
      this.discription,
      this.price,
      this.size,
      this.id,
      this.color});
}
